/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'cdn.ejemplo.com',
      'i.blogs.es',
      'i.postimg.cc'
    ],
  },
};

module.exports = nextConfig;
